<%= form_for(@fav_recipe) do |f| %>
  <% if @fav_recipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fav_recipe.errors.count, "error") %> prohibited this fav_recipe from being saved:</h2>

      <ul>
      <% @fav_recipe.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :recipe_id %><br>
    <%= f.text_field :recipe_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
